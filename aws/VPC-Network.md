# AWS VPC NetWork

### আউটবাউন্ড (Outbound)
আপনি বাড়ি থেকে বাইরে যান
**উদাহরণ:**

- আপনি ব্রাউজারে গুগল খুললেন → আপনার কম্পিউটার বাইরে গুগলের সার্ভারে রিকোয়েস্ট পাঠালো।
- মোবাইলে ফেসবুক অ্যাপ খুললেন → ডেটা বাইরের সার্ভারে রিকোয়েস্ট করলো।

**✅ মানে:**
আপনার ভিতর থেকে বাইরের দিকে যাওয়া ট্রাফিক = আউটবাউন্ড।
সাবনেটে 0.0.0.0/0 → IGW থাকলে এই আউটবাউন্ড ট্রাফিক ইন্টারনেটে যেতে পারে।

### ইনবাউন্ড (Inbound)
বাইরে থেকে কেউ আপনার বাড়িতে আসে

**উদাহরণ:**

- কেউ আপনার ওয়েবসাইট দেখতে আপনার সার্ভারে রিকোয়েস্ট পাঠালো → এই রিকোয়েস্ট বাইরে থেকে ভিতরে আসছে।
- কেউ আপনার শেয়ার করা ফাইল ডাউনলোড করতে আপনার কম্পিউটারে কানেক্ট করলো।

**✅ মানে:**
বাইরে থেকে আপনার ভিতরে আসা ট্রাফিক = ইনবাউন্ড।

**এক কথায়:**

- আপনি বাইরে কল করছেন → আউটবাউন্ড
- বাইরে থেকে আপনাকে কল আসছে → ইনবাউন্ড

### AWS VPC (Virtual Private Cloud)

AWS VPC (Virtual Private Cloud) হলো AWS-এর ভিতরে আপনার নিজের প্রাইভেট ভার্চুয়াল নেটওয়ার্ক 🛜
সহজভাবে বললে—এটা AWS cloud-এর মধ্যে আপনার নিজস্ব ডেটা সেন্টারের মতো।

###  AWS VPC Network কী?
VPC এমন একটা নেটওয়ার্ক যেখানে আপনি নিজেই ঠিক করেন—

- IP address range (CIDR)
- কোন সার্ভার পাবলিক হবে, কোনটা প্রাইভেট
- কে কোথা থেকে অ্যাক্সেস করতে পারবে
- ইন্টারনেট থাকবে কিনা

### VPC-এর প্রধান অংশগুলো CIDR (Classless Inter-Domain Routing) Block VPC-এর IP রেঞ্জ
```text
10.0.0.0/16
```

### Subnet Network কী?
`vpc ip range 10.0.0.0/16 (private ip)` VPC-এর ভিতরের ছোট নেটওয়ার্ক

- Public Subnet → Internet access আছে
- Private Subnet → Internet access নেই

উদাহরণ:
- Web Server → Public Subnet
- Database → Private Subnet 🔐

### 🔹 Public Subnet মানে কী?

**Public Subnet** মানে হলো—
👉 এই subnet-এর ভেতরের server ইন্টারনেট থেকে সরাসরি অ্যাক্সেসযোগ্য হতে পারে

```yaml

# Subnet + Route Table

Subnet: 10.0.1.0/24
Route Table:
0.0.0.0/0 → Internet Gateway
```

**📌 কিন্তু শর্ত আছে:**
- Subnet-এর Route Table এ থাকতে হবে
- 0.0.0.0/0 → Internet Gateway
- Instance-এর থাকতে হবে
- Public IP / Elastic IP
- Security Group-এ port open থাকতে হবে

### Private Subnet মানে কী?

**Private Subnet মানে—**
👉 এই subnet-এর server ইন্টারনেট থেকে সরাসরি অ্যাক্সেসযোগ্য নয়\
❌ Client computer থেকে direct access করা যাবে না

```yaml
Subnet: 10.0.2.0/24
Route Table:
(no Internet Gateway)
```

**কারণ:**
- Route table-এ Internet Gateway নাই
- Public IP নাই

**📌 উদাহরণ:**
- Database (MySQL / RDS)
- Internal API
- Backend services

📌 কিন্তু Private Subnet কি ইন্টারনেট ব্যবহার করতে পারে NAT Gateway ব্যবহার করে


